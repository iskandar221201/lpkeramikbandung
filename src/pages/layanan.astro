---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import { services } from "../data/services";
---

<Layout>
    <Navbar />

    <main class="pt-20">
        <!-- Hero Section -->
        <div class="bg-[#151515] py-20 text-center relative overflow-hidden">
            <div class="absolute inset-0 z-0 opacity-20">
                <img
                    src="https://images.unsplash.com/photo-1503387762-592deb58ef4e?q=80&w=1000&auto=format&fit=crop"
                    alt=""
                    class="w-full h-full object-cover grayscale"
                />
            </div>
            <div class="relative z-10 max-w-7xl mx-auto px-4">
                <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
                    Layanan & Solusi
                </h1>
                <p class="text-gray-400 text-lg max-w-2xl mx-auto">
                    Keramik Bangunan Bandung (KKB) menghadirkan layanan
                    konstruksi terintegrasi untuk menjawab segala kebutuhan
                    bangunan Anda.
                </p>
            </div>
        </div>

        <!-- Services Grid -->
        <section class="py-24 bg-white">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div
                    class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8"
                >
                    {
                        services.map((service) => (
                            <div class="bg-gray-50 rounded-2xl p-8 shadow-sm hover:shadow-xl transition-all duration-300 border border-gray-100 group flex flex-col h-full">
                                <div class="w-14 h-14 bg-[#FE5C01]/10 rounded-xl flex items-center justify-center mb-6 group-hover:bg-[#FE5C01] transition-colors duration-300 flex-shrink-0">
                                    <svg
                                        class="w-8 h-8 text-[#FE5C01] group-hover:text-white transition-colors duration-300"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                        set:html={service.icon}
                                    />
                                </div>
                                <h4 class="text-xl font-bold text-[#151515] mb-3">
                                    {service.title}
                                </h4>
                                <p class="text-gray-600 leading-relaxed text-sm flex-grow">
                                    {service.description}
                                </p>
                            </div>
                        ))
                    }
                </div>
            </div>
        </section>

        <!-- Workflow / Process Section -->
        <section class="py-24 bg-gray-900 text-white">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="text-center max-w-3xl mx-auto mb-16">
                    <h2
                        class="text-[#FE5C01] font-semibold tracking-wide uppercase text-sm mb-2"
                    >
                        Alur Kerja
                    </h2>
                    <h3 class="text-3xl font-bold mb-4">
                        Bagaimana Kami Bekerja
                    </h3>
                    <p class="text-gray-400">
                        Proses sistematis untuk memastikan proyek Anda berjalan
                        lancar dari awal hingga akhir.
                    </p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
                    <!-- Step 1 -->
                    <div class="relative">
                        <div
                            class="text-5xl font-bold text-[#FE5C01]/20 mb-4 absolute -top-8 -left-4"
                        >
                            01
                        </div>
                        <h4 class="text-xl font-bold mb-3 relative z-10">
                            Konsultasi
                        </h4>
                        <p class="text-gray-400 text-sm relative z-10">
                            Diskusi mendalam mengenai kebutuhan, anggaran, dan
                            visi proyek Anda.
                        </p>
                    </div>
                    <!-- Step 2 -->
                    <div class="relative">
                        <div
                            class="text-5xl font-bold text-[#FE5C01]/20 mb-4 absolute -top-8 -left-4"
                        >
                            02
                        </div>
                        <h4 class="text-xl font-bold mb-3 relative z-10">
                            Perencanaan
                        </h4>
                        <p class="text-gray-400 text-sm relative z-10">
                            Pembuatan desain, RAB, dan jadwal kerja yang
                            terperinci.
                        </p>
                    </div>
                    <!-- Step 3 -->
                    <div class="relative">
                        <div
                            class="text-5xl font-bold text-[#FE5C01]/20 mb-4 absolute -top-8 -left-4"
                        >
                            03
                        </div>
                        <h4 class="text-xl font-bold mb-3 relative z-10">
                            Eksekusi
                        </h4>
                        <p class="text-gray-400 text-sm relative z-10">
                            Pelaksanaan konstruksi oleh tim ahli dengan
                            pengawasan ketat.
                        </p>
                    </div>
                    <!-- Step 4 -->
                    <div class="relative">
                        <div
                            class="text-5xl font-bold text-[#FE5C01]/20 mb-4 absolute -top-8 -left-4"
                        >
                            04
                        </div>
                        <h4 class="text-xl font-bold mb-3 relative z-10">
                            Serah Terima
                        </h4>
                        <p class="text-gray-400 text-sm relative z-10">
                            Final check dan penyerahan kunci proyek yang telah
                            selesai sempurna.
                        </p>
                    </div>
                </div>
            </div>
        </section>

        <!-- CTA -->
        <section class="py-16 bg-[#FE5C01]">
            <div class="max-w-7xl mx-auto px-4 text-center">
                <h2 class="text-3xl font-bold text-white mb-6">
                    Butuh Layanan Khusus?
                </h2>
                <p class="text-white/90 text-lg mb-8 max-w-2xl mx-auto">
                    Kami fleksibel dan siap menyesuaikan layanan dengan
                    kebutuhan spesifik proyek Anda.
                </p>
                <a
                    href="/#contact"
                    class="inline-block bg-white text-[#FE5C01] font-bold py-3 px-8 rounded-full hover:bg-gray-100 transition-colors shadow-lg"
                >
                    Diskusikan Sekarang
                </a>
            </div>
        </section>
    </main>

    <Footer />
</Layout>
